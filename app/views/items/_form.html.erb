<h1><%= @item.id ? ' Edit Item ' : 'Create Item' %></h1>
<hr />
<%= form_for @item do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name, required: true, autofocus: true %>

<%= f.label :name %>
<%= f.text_field :price %>

<%= f.label :name %>
<%= f.select :available %>

  <%= f.submit %>
<% end %>